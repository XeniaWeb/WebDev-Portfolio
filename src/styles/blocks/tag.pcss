.tag {
  display: block;
  font-size: 15px;
  line-height: 30px;
  font-weight: 600;
  color: rgba($text-color, 0.6);
  background: #e6e6e6;
  padding: 10px 20px;
  border-radius: 30px;
}

@include phones {
  .tag {
    font-size: 13px;
    line-height: 24px;
    padding: 5px 12px;
  }
}